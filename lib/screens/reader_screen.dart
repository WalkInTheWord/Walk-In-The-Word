import 'package:flutter/material.dart';

class ReaderScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Bible Reader')),
      body: Center(child: Text('Bible Reading Interface')),
    );
  }
}